name: Windows CI

on:
  push:
    branches: [main]
  # Every pull request, not only those targeting main. Work is sequenced as a
  # stack of small branches, and a pull request from one package branch onto
  # another got no CI at all while this filtered on main.
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-and-build:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        shell: pwsh
        run: python -m pip install -r requirements.txt

      - name: Validate source and data
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $env:PYTHONPATH = Join-Path $PWD 'src'

          python -m compileall -q src tools launcher.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

          Get-ChildItem models -Recurse -Filter model.json -File | ForEach-Object {
            python tools/validate_model.py $_.FullName
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }

          Get-ChildItem presets -Filter *.json -File | ForEach-Object {
            python tools/validate_preset.py $_.FullName
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }

          python tools/model_variations_smoke.py
          python tools/color_override_smoke.py
          python tools/progression_smoke.py
          python tools/personality_catalog_smoke.py
          python tools/phase_scheduler_smoke.py
          python tools/jobs_smoke.py
          python tools/job_blend_smoke.py
          python tools/drifter_smoke.py
          python tools/inspector_ui_smoke.py
          python tools/preset_abilities_smoke.py
          python tools/state_migration_smoke.py
          python tools/spider_movement_smoke.py
          python tools/tarantula_smoke.py
          python tools/chosen_one_smoke.py

      - name: Build Windows executable smoke test
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          python -m PyInstaller `
            --noconfirm `
            --clean `
            --onefile `
            --windowed `
            --name DesktopBugCompanion `
            --distpath ci-dist `
            --workpath ci-build `
            --paths src `
            --add-data 'models;models' `
            --add-data 'personalities;personalities' `
            --add-data 'presets;presets' `
            --hidden-import desktop_bug.engine `
            launcher.py

          if (-not (Test-Path -LiteralPath 'ci-dist/DesktopBugCompanion.exe')) {
            throw 'PyInstaller did not produce ci-dist/DesktopBugCompanion.exe'
          }
